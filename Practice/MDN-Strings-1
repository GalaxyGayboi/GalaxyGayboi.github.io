https://developer.mozilla.org/en-US/docs/Learn/JavaScript/First_steps/Test_your_skills:_Strings

Answer 1 

let quoteStart = "Don't judge each day by the harvest you reap";
let quoteEnd = " but by the seeds you plant.";
let finalQuote = quoteStart + quoteEnd;
quoteStart;

Answer 2

quoteLength = quote.length;
index = quote.indexOf(substring);
revisedQuote =quote.replace('. I do not like them, Sam-I-Am', '');

Answer 3

quote = quote.slice(1,40)
fixedQuote = "I" + quote.toLowerCase();
finalQuote = fixedQuote.replace("green eggs and ham", "beans") + ".";

Answer 4

let theorem = `Pythagorean theorem`;

let a = 5;
let b = 8;

let myString = `Using the ${theorem}, we can work out that that if the two shortest sides of a right-angled triangle have lengths of ${a} and ${b}, the length of the hypotenuse is ${Math.sqrt(a ** 2 + b ** 2)}`;
